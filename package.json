{
  "name": "printforge",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "backend"
  ],
  "scripts": {
    "dev:local": "bash scripts/dev-local.sh",
    "deploy:server": "bash scripts/deploy-server.sh",
    "dev": "concurrently -n backend,configurator,admin -c blue,green,yellow \"npm run dev:backend\" \"npm run dev:configurator\" \"npm run dev:admin\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:admin": "npm run dev --workspace=apps/admin",
    "dev:configurator": "npm run dev --workspace=apps/configurator",
    "build": "npm run build --workspace=apps/configurator && npm run build --workspace=apps/admin"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
